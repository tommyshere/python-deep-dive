# only Python 3
sorted(iterable, key=lambda x: x, reverse=False)
# key is a function that returns the values
# so lambda is mucho important
# this returns a new instance

# list has a .sort(key= lambdax: x, reverse=False) method that mutates
l.sort()